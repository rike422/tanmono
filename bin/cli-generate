#!/usr/bin/env node

const program = require('commander');

program
  .command('component [name]', "generate components").alias('c')
  .command('storybook', "generate react-story-book.config").alias('s')
  .parse(process.argv);
