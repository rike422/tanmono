#!/usr/bin/env node

const program = require('commander');

program
  .version('0.0.1')
  .command('generate [name]', "generate components")
  // .alias('g')
  // .description('Generate new react component')
  .parse(process.argv);
