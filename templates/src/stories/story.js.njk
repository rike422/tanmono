import React from 'react';
import { storiesOf, action } from '@kadira/storybook';
import {{ className }} from '../{{ directory }}/{{ baseName }}';

storiesOf('{{ className }}', module)
  .add('render', () => <{{ className }}></{{ className }}>)


